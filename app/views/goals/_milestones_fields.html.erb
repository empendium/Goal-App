<div id="milestones">
<p>
   <%= f.label :description, "Milestone"%><br />
   <%= f.text_area :description, :rows => 2 %><br />
   <%= f.label :completion_date,"Complete by" %><br />
   <%= f.date_select :completion_date %>
 </p>
</div>
 <%= f.fields_for :steps do |builder|%>
   <%= render 'steps_fields', :f => builder %>
 <%end%>
 <%=link_to "add a step", "#", :class => "add_step"%>